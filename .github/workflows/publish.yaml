name: Publish

on:
  workflow_call:
    inputs:
      tag_name:
        required: true
        type: string
    secrets:
      publish_token:
        required: true
  workflow_dispatch:
    inputs:
      tag_name:
        description: 'tag_name'
        required: true
        type: string
      registry:
        description: 'bazel central registry target'
        required: false
        default: 'fweikert/bazel-central-registry'
        type: string
      registry-fork:
        description: ''
        required: true
        default: publish-to-bcr-dev/bazel-central-registry
        type: string

permissions:
  id-token: write
  attestations: write
  contents: write

jobs:
  publish:
    uses: bazel-contrib/publish-to-bcr/.github/workflows/publish.yaml@d4d4f08c0eb920212e51de2483b29b69cf1bfe27
    with:
      tag_name: ${{ inputs.tag_name }}
      registry: ${{ inputs.registry }}
      registry_fork: ${{ inputs.registry_fork }}
      open_pull_request: false
      attest: true
    secrets:
      publish_token: ${{ secrets.PUBLISH_TOKEN }}
